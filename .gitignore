# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# e2e artefacts
scripts/e2e/out
node_modules_pw

# Separate projects that live beside this one: an older landing site, and the
# desktop app, which has its own repository.
# Anchored with a slash: an unanchored `app` also matches web/app, the site's
# Next.js directory - it was silently left out of the repository and the first
# Pages build died looking for a file that had never been pushed.
/site
/app

# The site is here rather than out: GitHub Pages builds it from this repository,
# and keeping it alongside the player is what makes the version it advertises
# checkable against the version being shipped. Its own .gitignore covers
# everything scripts/sync-player.mjs copies in.

# Claude Code configuration is per-developer and lives in ~/.claude at user
# scope. Nothing about how the repository is edited belongs in the repository.
.claude/
