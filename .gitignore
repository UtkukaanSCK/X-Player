# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# e2e artefacts
scripts/e2e/out
node_modules_pw

# Separate projects that live beside this one: an older landing site, and the
# desktop app, which has its own repository.
site
app

# The site is not ignored: GitHub Pages builds it from this repository, and
# keeping it beside the player is what makes the version it advertises
# checkable against the version being shipped. Its own .gitignore covers
# everything scripts/sync-player.mjs copies in.
